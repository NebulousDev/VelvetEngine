package graphics;

public class GLGraphics implements IGraphics
{
	@Override
	public GraphicsBuffer createBuffer(GraphicsBufferType type, int size)
	{
		return new GraphicsBuffer(0, size, type);
	}

}
